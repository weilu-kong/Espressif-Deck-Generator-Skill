# Branding assets

Place approved official brand assets here when a project requires them. The default renderer intentionally uses a text/shape brand mark so the skill does not depend on a missing external logo file.
